package tests;

import com.hirshi001.networking.packet.Packet;

/**
 * A class that verifies that the packet is valid.
 */
public class PacketVerifier {

    /**
     * Verifies that the packet is valid.
     * @param packet the packet
     * @return true if the packet is valid, false otherwise
     */
    public boolean verifyPacket(Packet packet) {
        /*
        * This method should be used to verify that the packet is valid.
        * There is no implementation as of now
         */
        return true;
    }

}
